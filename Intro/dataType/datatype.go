package main  

func main()  {   
	// COMMON DATA TYPES   
	// string 
	// int (integer)--both positive and negative numbers 
	// Two types of int-- 1.signed int(int)--can hold both positive and negative integers while unsigned integer(uint)--can only hold positive integers   
	// int/uint---size is -- either 32bits(4bytes) or 64bits(8bytes) 
	// int8/uint8--size is-- either 8bits(1 byte )
	// float--for numbers with decimal points.Divided in to float32(32bits(4bytes)) and float64(64bits(8bytes)).
	// Float32--store value of decimal to 3 decimal places while float64 stores to precision(Memory expensive).its the default float when not declared 
	// COMPLEX Numbers i.e 3 + 4i , -9.5+18.3i  
	// bool i.e true or false 
	// byte(8bits)--used for non-negative numbers 
	// rune--used for characters internally used as 32-bit integers i.e "a","7","<"  
	// Array --
}   
        
